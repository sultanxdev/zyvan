{
  "name": "@zyvan/crypto",
  "version": "0.1.0",
  "private": true,
  "description": "Zyvan cryptographic utilities — HMAC signing, encryption, API key hashing, SSRF protection",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.1.0"
  }
}
