{
  "name": "@zyvan/database",
  "version": "0.1.0",
  "private": true,
  "description": "Zyvan database package — Prisma schema, client, and migrations",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "generate": "prisma generate",
    "migrate": "prisma migrate dev",
    "migrate:prod": "prisma migrate deploy",
    "push": "prisma db push",
    "studio": "prisma studio",
    "seed": "ts-node prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.8.0"
  },
  "devDependencies": {
    "prisma": "^6.8.0",
    "typescript": "^5.7.0",
    "ts-node": "^10.9.0"
  }
}
